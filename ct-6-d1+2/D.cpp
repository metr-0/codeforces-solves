#include <cstdio>

#define S 200005

int c[S];

int rec(int amount) {
    
}

void solve() {
    int n; scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &c[i]);
    }
}

int main() {
    int tc; scanf("%d", &tc);
    while (tc--) solve();
}

